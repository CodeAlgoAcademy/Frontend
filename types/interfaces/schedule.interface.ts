export interface Schedule {
    allSchedule: [];
    googleConnect: boolean;
 }

 export interface ISchedule {
    id: string;
    Subject: string;
    location: string;
    startTime: string;
    endTime: string;
 }